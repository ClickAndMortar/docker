apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/name: elasticsearch
    role: master
  ports:
  - name: http
    port: 9200
